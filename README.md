# Examen-de-Ingenieria-de-Software
Este repositorio corresponde a las actividades desarrolladas durante la asignatura de Ingeniería de Software.
git add README.md
git commit -m "Primer commit: agregar README del proyecto"
git --version

git config --global user.name "Roger Roberto Beltran Palma"
git config --global user.email "TU_CORREO"

mkdir proyecto-github
cd proyecto-github

git init

git add README.md
git commit -m "Primer commit: agregar README del proyecto"

git branch -M main

git remote add origin https://github.com/TU-USUARIO/TU-REPOSITORIO.git

git remote -v

git push -u origin main
